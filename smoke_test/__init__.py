"""
End-to-end smoke test: a real app process, a fake networked receipt printer.

Run it with `bin/smoke-test`. The default `pytest` deselects it (see the
addopts in pyproject.toml) so that bin/test and the pre-commit hook stay quick.
"""
